spring:
  application:
    name: producer
  datasource:
    url: jdbc:postgresql://localhost:6000/job_queue_db
    username: postgres
    password: postgres
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: false
  liquibase:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

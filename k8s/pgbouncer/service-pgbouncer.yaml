apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: distributed-job-queue
spec:
  type: NodePort
  selector:
    app: pgbouncer
  ports:
    - port: 6432
      targetPort: 6432
      nodePort: 30432
